import { useState, useEffect, useRef } from "react";
import { Chart as ChartJS, registerables, type ChartOptions, type ChartConfiguration, type ChartData } from "chart.js";
import { Bar } from "react-chartjs-2";
import { CashflowMock } from "@/app/lib/types";

ChartJS.register(...registerables);

const CashflowSummaryChart = () => {
  return (
    <>
      
    </>
  )
}

export default CashflowSummaryChart;